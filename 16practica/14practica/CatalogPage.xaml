<Window x:Class="MediaLibraryApp.CatalogPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MediaLibraryApp - Каталог"
        Width="1200"
        Height="760"
        Style="{StaticResource WindowStyle}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Margin="0,0,0,20"
                Padding="24,18"
                Background="{StaticResource SecondaryBackgroundBrush}"
                CornerRadius="18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Каталог медиаконтента" Style="{StaticResource HeaderTextStyle}"/>
                    <TextBlock Margin="0,6,0,0"
                               Text="Карточки загружаются из базы данных"
                               Style="{StaticResource SubHeaderTextStyle}"/>
                </StackPanel>
                <Button Grid.Column="1"
                        Width="120"
                        Content="Выйти"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="LogoutButton_Click"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ListView x:Name="CatalogListView"
                      Background="Transparent"
                      BorderThickness="0"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Margin" Value="0,0,16,16"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Width="260"
                                Padding="14"
                                Background="{StaticResource SecondaryBackgroundBrush}"
                                CornerRadius="16">
                            <StackPanel>
                                <Grid Height="146">
                                    <Image Source="{Binding Poster}" Stretch="UniformToFill">
                                        <Image.Style>
                                            <Style TargetType="Image">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasImage}" Value="False">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                    <Border Background="#33555555">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasImage}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   FontSize="13"
                                                   Foreground="{StaticResource MutedTextBrush}"
                                                   Text="Нет изображения"/>
                                    </Border>
                                </Grid>

                                <TextBlock Margin="0,12,0,6"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource PrimaryTextBrush}"
                                           Text="{Binding Title}"/>

                                <TextBlock Height="42"
                                           TextWrapping="Wrap"
                                           Foreground="{StaticResource MutedTextBrush}"
                                           Text="{Binding ShortDescription}"/>

                                <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                                    <Border Padding="8,3" Background="#33FFFFFF" CornerRadius="10">
                                        <TextBlock FontSize="11"
                                                   Foreground="{StaticResource PrimaryTextBrush}"
                                                   Text="{Binding StatusName}"/>
                                    </Border>
                                    <Border Margin="8,0,0,0" Padding="8,3" Background="#2234A853" CornerRadius="10">
                                        <TextBlock FontSize="11"
                                                   Foreground="{StaticResource PrimaryTextBrush}"
                                                   Text="{Binding RatingText}"/>
                                    </Border>
                                    <Border Margin="8,0,0,0" Padding="8,3" Background="#22339AF0" CornerRadius="10">
                                        <TextBlock FontSize="11"
                                                   Foreground="{StaticResource PrimaryTextBrush}"
                                                   Text="{Binding Difficulty}"/>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
    </Grid>
</Window>
